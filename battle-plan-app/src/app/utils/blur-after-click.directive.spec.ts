import { BlurAfterClickDirective } from './blur-after-click.directive';

describe('BlurAfterClickDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurAfterClickDirective();
    expect(directive).toBeTruthy();
  });
});
